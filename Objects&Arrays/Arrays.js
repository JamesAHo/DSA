// Big O of Arrays
// insertion => depends
// removal -> depends
// Searching -> O(N)
// Access - O(1)

let names = ["James", "Jenny", "Xoi"]

names.pop()
// Big O of Array Operations

// push() pop() => O(1)
// shift() unshift() =>  O(n)
// sort => O(N * Log N)
